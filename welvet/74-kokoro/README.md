# 74. Kokoro — reserved empty slots

**When:** you specifically want Kokoro TTS.  
**Where:** `welvet/model/kokoro` and `welvet/apps/kokoro` — **both empty placeholders** today.  
**Why:** names reserved for a future port; do not import them yet.

Working speech apps right now:

| App | Role |
|-----|------|
| `apps/mosstts` | Moss TTS shell |
| `apps/qwentts` | Qwen TTS shell |

```bash
cd 74-kokoro && source ../env.sh && go run .
```
