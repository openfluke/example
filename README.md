# openfluke/example

Public runnable examples for Welvet / Tide / friends.

## Cameral / Parallel

See **[`cam/`](cam/)** — every cam feature (modes, combine, CamSync, CamKit) and every
layer-as-cam, with READMEs for when / where / why.

```bash
cd cam && go run ./cmd/runall
```
